[uwsgi]
project = zuipython
#env = my_project_env
base = /home/ubuntu

http = :8001

chdir = %(base)/%(project)
#home = %(base)/%(env)
module = %(project).wsgi:application

processes = 4
master = true
vacuum = true

daemonize = %(base)/%(project)uwsgi.log
